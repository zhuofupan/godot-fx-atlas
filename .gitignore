# 使用方工程导入的数据包 —— 不进版本库
#
# 工作台（workbench.html）是项目无关的工具：它只认数据包，数据包由使用方工程的导出器生成。
# 数据包通常含有该工程的名称、条目命名、素材路径与截图，属于**私有内容**，
# 提交即公开（本仓库是公开仓库）。所以这里整目录排除。
#
# 详见 docs/workbench.md
project-data/

# 由 project-data/ 编译出来的前端数据集（含上面的私有内容，同样不进库）
assets/workbench-data.js

# 本地开发产物
node_modules/
.DS_Store
Thumbs.db

# 探针脚本的临时产物
__*.html
__*.png
